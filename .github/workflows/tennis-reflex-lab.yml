name: Build Tennis Reflex Lab APK

on:
  workflow_dispatch:
  push:
    branches:
      - tennis-reflex-lab-native
    paths:
      - 'TennisReflexLab/**'
      - '.github/workflows/tennis-reflex-lab.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Install Android SDK packages
        run: |
          yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --licenses >/dev/null || true
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "platforms;android-35" "build-tools;35.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build debug APK
        working-directory: TennisReflexLab
        run: gradle --no-daemon :app:assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: tennis-reflex-lab-debug
          path: TennisReflexLab/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
